class Constraints {
    constructor() {
    }

    static module(list, timeDelta) {
        // for (var [id, obj] of list.entries()) {
        //     var length = Vector2d.lengthSqrt()
        // }
    }
}