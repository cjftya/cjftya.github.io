class AbsScene extends ViewGroup {
    constructor(context) {
        super(context);
    }

    getName() {
        return "AbsScene";
    }

    // addChild(view) {
    //     this.getContext().stage.addChild(view);
    // }
}